#include <stdio.h>
#include <math.h>
void main()
{
  float R;
  int x,y,h;
  scanf("%i\n%i\n%i", &x, &y, &h);
  if (y>=0)
  {
      R=(0,33*h*h-4,44)/(exp(y+h)+pow((2,7*y),(0,5)));
  }
  printf("%f\n", R);
}
